#pragma once

#include "ModSettings.h"

namespace Crucible
{
	/*
	float SettingBase::GetValue(SettingType getSetting)
	{
		return -1;
	}

	float RecoverySetting::GetValue(SettingType getSetting)
	{
		switch (getSetting)
		{
		case SettingType::recoveryRate:
			return recoveryRate;
		case SettingType::recoveryMin:
			return recoveryMin;
		default:
			return __super::GetValue(getSetting);
		}
	}
	//*/
}